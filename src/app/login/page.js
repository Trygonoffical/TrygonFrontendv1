import LoginPage from '../../components/pages/LoginPage';

export const metadata = {
  title: 'Login - Trygon Client Portal',
  description: 'Access your Trygon client dashboard to track projects, view invoices, and communicate with our team.',
};

export default function Login() {
  return <LoginPage />;
}