import AdminDashboard from '../../components/pages/AdminDashboard';

export const metadata = {
  title: 'Admin Dashboard - Trygon Management Portal',
  description: 'Administrative dashboard for managing projects, clients, and business operations at Trygon.',
};

export default function Admin() {
  return <AdminDashboard />;
}